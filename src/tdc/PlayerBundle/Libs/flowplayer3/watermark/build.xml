<project name="TDC Watermark" default="deploy">
    
    <property name="devkit-dir" value="../flowplayer.devkit" />

	<property file="${devkit-dir}/plugin-build.properties" />
	<import file="${devkit-dir}/plugin-build.xml"/>
	<property name="flowplayer_lib" value="${devkit-dir}/flowplayer.swc" />
	
	<property name="version" value="0.6" />
	<property name="basename" value="tdc.watermark" />
    <property name="releasedir" value="${basename}" />
	<property name="plugin-binary" value="${basename}.swf" />
	<property name="plugin-binary-versioned" value="${basename}-${version}.swf" />
	<property name="plugin-main-class" value="org/tdc/watermark/Watermark.as" />
	<property name="library-path" value="${flex3dir}/frameworks/locale/en_US" />

</project>
